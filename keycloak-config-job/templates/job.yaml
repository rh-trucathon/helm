apiVersion: batch/v1
kind: Job
metadata:
  name: keycloak-config-job
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      annotations:
        argocd.argoproj.io/sync-wave: "0"
    spec:
      serviceAccountName: keycloak-config-job-runner
      restartPolicy: Never
      containers:
      - name: keycloak-config
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        command:
          - /bin/bash
          - '-c'
          - |
            set -e
            set -x

            # Change to the ansible directory
            cd /tmp/ansible
            
            # Run the ansible playbook
            echo "Running ansible playbook for cluster: ${CLUSTER}"
            ansible-playbook -i localhost playbook.yaml \
              -e cluster=${CLUSTER} \
              -e keycloak_namespace=${KEYCLOAK_NAMESPACE:-rhsso} \
              -e keycloak_realm=${KEYCLOAK_REALM:-openshift}
            
            echo "Keycloak configuration completed successfully"
        envFrom:
        - configMapRef:
            name: keycloak-config-job-config
        volumeMounts:
        - name: ansible
          mountPath: /tmp/ansible
          readOnly: true
      volumes:
      - name: ansible
        configMap:
          name: keycloak-config-job-playbook
          items:
          - key: playbook.yaml
            path: playbook.yaml

